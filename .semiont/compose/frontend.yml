# Semiont frontend
#
# Usage:
#   docker compose -f .semiont/compose/frontend.yml up
#
# Requires the backend running on http://localhost:4000.

services:
  frontend:
    build:
      context: ../..
      dockerfile: .semiont/containers/Dockerfile.frontend
    ports:
      - "3000:3000"
